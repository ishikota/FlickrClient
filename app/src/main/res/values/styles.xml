<resources>

    <!-- Theme for -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/actionbar_color</item><!-- default action bar background -->
        <item name="colorPrimaryDark">@color/primary_dark</item> <!-- used for the status bar -->
        <item name="colorAccent">@color/accent</item> <!-- accent color is also used to tint widgets -->
        <item name="android:textColorPrimary">@color/text_primary</item> <!-- the color of the tile text-->
        <item name="android:windowBackground">@color/white_bg</item>
    </style>

    <!-- this style is used in toolbar xml -->
    <style name ="LightPopupMenu" parent="android:Theme.Holo.Light"/>

    <style name="DetailTitle">
        <item name="android:text">Treats at Dandelion \nChocolate"</item>
        <item name="android:textColor">#FFF</item>
        <item name="android:textSize">24sp</item>
        <item name="android:background">@color/actionbar_color</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingBottom">56dp</item>
    </style>

</resources>
